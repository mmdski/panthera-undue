app = executable('app', 'app.c', include_directories : inc,
                 link_with : [ciilib, pantheralib])
reachtree = executable('reachtree', 'reachtree.c', include_directories : inc,
                       link_with : [ciilib, pantheralib])
